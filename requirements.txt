# Polymarket BTC 15-Minute Trading Bot - Dependencies
# Install with: pip install -r requirements.txt

# Environment variable management
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# HTTP client with better features (used for Polymarket page scraping)
httpx>=0.25.0

# Data analysis & manipulation (Required for backtesting)
pandas>=2.0.0
numpy>=1.24.0

# Plotting (Optional, for backtest visualization)
matplotlib>=3.7.0

# Web3 for on-chain redemption of winning positions (REQUIRED for live trading)
web3>=6.0.0

# Optional but recommended for better async support
# (not used in v1, but useful for future extensions)
# aiohttp>=3.9.0

# For Polymarket CLOB API interactions
# Polymarket official Python client (v2 required for CLOB v2 order signing)
py-clob-client-v2>=1.0.1

# ─────────────────────────────────────────────────────────────────────────────────
# LIVE TRADING DEPENDENCIES (Required for TEST_MODE=false)
# ─────────────────────────────────────────────────────────────────────────────────
# Ethereum account management and signing
eth-account>=0.10.0

# Web3 library for blockchain interactions
web3>=6.0.0
